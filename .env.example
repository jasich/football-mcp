# Base URL for the MCP server (used in widget component loading)
# For local development with ngrok:
# BASE_URL=https://your-ngrok-url.ngrok-free.app
# For Cloudflare Tunnel:
# BASE_URL=https://your-subdomain.yourdomain.com
# For production:
# BASE_URL=https://your-domain.com
BASE_URL=http://localhost:3000

# Optional: Custom allowed host for Rails (e.g., Cloudflare Tunnel domain)
# Leave empty to only allow localhost and ngrok domains
# ALLOWED_HOST=your-subdomain.yourdomain.com
